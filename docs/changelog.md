## (`01.06.2026`) 3.2.1.9 build 3366:
- Удален ненужный файл `icon.png`.

## (`31.05.2026`) 3.2.1.9 build 3365:
- Добавление **SoundCloud**, **Meta** доменов перенесено в `list-custom.txt`.

## (`30.05.2026`) 3.2.1.9 build 3364:
- Добавлена кнопка **показа**/**скрытия** инструкции для подключения **Telegram Proxy**;
- Удалено логирование действий, связанных с файлом `list-custom.txt`;
- Порт **Telegram Proxy** изменен с `1080` (**SOCKS5**) на `1443` (**HTTPS**);
- Исправлена проблема с конфликтом **уведомлений**.

## (`28.05.2026`) 3.2.1.9 build 3363:
- Добавлено автоматическое создание файла `list-custom.txt` при его отсутствии;
- Переработан метод **запуска лаунчера** после его обновления через **splash окно**.

## (`28.05.2026`) 3.2.1.9 build 3362:
- Добавлен новый `list-custom.txt`, поддерживает кастомные домены сайтов (**не** обновляется при **новых обновлениях**);
- Переделано **splash окно**.

## (`27.05.2026`) 3.2.1.9 build 3361:
- Добавлена поддержка **украинского** языка;
- Исправлена смена **темы оформления**;
- Исправлена смена **языка интерфейса**.
